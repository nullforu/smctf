apiVersion: v1
kind: Pod
metadata:
  name: xss-filtering-bypass-pod
spec:
  containers:
    - name: xss-filtering-bypass-container
      image: 986129558966.dkr.ecr.ap-northeast-2.amazonaws.com/web-xss-filtering-bypass:latest
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 8000
      resources:
        limits:
          memory: "128Mi"
          cpu: "100m"
        requests:
          memory: "128Mi"
          cpu: "100m"
