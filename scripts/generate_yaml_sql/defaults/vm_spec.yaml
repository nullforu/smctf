apiVersion: v1
kind: Sandbox
metadata:
  name: challenge-sandbox
spec:
  containers:
    - name: challenge
      image: alpine:3.19
      command: ["sleep", "3600"]
      ports:
        - containerPort: 80
